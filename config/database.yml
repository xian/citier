# SQLite version 3.x
#   gem install sqlite3
# development:
  # adapter: sqlite3
  # database: db/development.sqlite3
  # pool: 5
  # timeout: 5000
# 
# # Warning: The database defined as "test" will be erased and
# # re-generated from your development database when you run "rake".
# # Do not set this db to the same as development or production.
# test:
  # adapter: sqlite3
  # database: db/test.sqlite3
  # pool: 5
  # timeout: 5000
# 
# production:
  # adapter: sqlite3
  # database: db/production.sqlite3
  # pool: 5
  # timeout: 5000

development:
    adapter: mysql2
    database: citier_development
    encoding: utf8
    username: root
    password: root
    host: localhost

test:
    adapter: mysql2
    database: citier_test
    encoding: utf8
    username: root
    password: root
    host: localhost

production:
    adapter: mysql2
    database: citier_production
    encoding: utf8
    username: root
    password: root
    host: localhost